name: Auto Release
on:
  schedule:
    - cron: '0 0 * * 0'  # Every Sunday

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create release
        run: node scripts/gh-release.js
