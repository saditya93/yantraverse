{
  "date": "2026-09-15",
  "category": "FEATURE",
  "title": "Add built‑in request validation middleware",
  "description": "Introduces a lightweight, zero‑dependency validation layer that checks request bodies, query strings, and params against user‑defined schemas before they reach route handlers. This prevents malformed data from propagating, reduces boilerplate validation code, and improves security and developer ergonomics.",
  "complexity": 7,
  "estimatedLoc": 180,
  "filesAffected": [
    "src/middleware/validate.js",
    "src/index.js",
    "test/validate.test.js"
  ],
  "testCases": [
    "Validate that a request with a valid JSON body passes through and reaches the handler",
    "Validate that a request with an invalid body returns a 400 response with detailed error information"
  ],
  "acceptance": "When the middleware is registered with a schema, valid requests are processed normally, while invalid requests receive a 400 Bad Request with a JSON error payload; middleware integrates seamlessly with existing routing and does not affect performance beyond negligible overhead.",
  "reason": "Current framework lacks systematic input validation, leading developers to write repetitive checks or rely on external libraries, increasing code size and risk of bugs. Adding native validation aligns with the framework's zero‑dependency ethos while delivering high‑impact safety and productivity gains."
}