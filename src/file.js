 // Import required modules const express = require('express') const multer = require('multer') const bodyParser = require('body-parser') /**  * Request Body Parser  *   * Handles JSON, URL-encoded, and multipart/form-data request bodies  *   * @param {express.Application} app - Express application instance  */ function initRequestBodyParser(app) {   // Set up JSON body parser   const jsonParser = bodyParser.json()      // Set up URL-encoded body parser   const urlencodedParser = bodyParser.urlencoded({ extended: true })      // Set up multipart/form-data body parser   const upload = multer()      // Apply body parsers to all routes   app.use(jsonParser)   app.use(urlencodedParser)   app.use(upload.any()) } /**  * Get request body  *   * @param {express.Request} req - Express request object  * @returns {object} Request body  */ function getRequestBody(req) {   // Check if request body exists   if (!req.body) {     throw new Error('Request body is empty')   }      return req.body } // Export functions module.exports = {   initRequestBodyParser,   getRequestBody } 