   // Import required modules   const yantraverse = require('yantraverse')   // Define a function to perform the demo feature   /**    * Performs the demo feature    * @returns {string} A message indicating the demo feature has been performed    */   function performDemoFeature() {     // Define a variable to store the API key from GitHub Secrets     const groqApiKey = process.env.GROQ_API_KEY     // Check if the API key is set     if (groqApiKey) {       // Perform the demo feature using the API key       return 'Demo feature performed using GROQ API key'     } else {       // Return a message indicating the API key is not set       return 'GROQ API key is not set'     }   }   // Export the function   module.exports = performDemoFeature 