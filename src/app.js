 // Import required modules const http = require('http') const https = require('https') const express = require('express') // Create an Express application const app = express() /**  * Redirects HTTP requests to HTTPS  * @param {http.IncomingMessage} req - The incoming HTTP request  * @param {http.ServerResponse} res - The outgoing HTTP response  */ function redirectHttps(req, res) {   // Check if the request is already using HTTPS   if (req.headers['x-forwarded-proto'] === 'https' || req.protocol === 'https') {     // If already using HTTPS, do not redirect     return   }   // Redirect to the same URL using HTTPS   const httpsUrl = `https://${req.headers.host}${req.url}`   res.redirect(301, httpsUrl) } // Use the redirect function as middleware app.use(redirectHttps) // Start the Express server const port = 80 app.listen(port, () => {   console.log(`Server listening on port ${port}`) }) 