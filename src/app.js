 // src/errorHandler.js /**  * ErrorHandler class for centralized error handling  */ class ErrorHandler {   /**    * Constructor for ErrorHandler    * @param {Object} logger - logger instance    */   constructor(logger) {     this.logger = logger   }   /**    * Handle error and log it    * @param {Error} error - error to handle    * @param {Object} req - request object    * @param {Object} res - response object    */   handleError(error, req, res) {     const errorMessage = error.message     const errorStack = error.stack     this.logger.error(errorMessage)     this.logger.error(errorStack)     res.status(500).json({ message: 'Internal Server Error' })   }   /**    * Handle route not found error    * @param {Object} req - request object    * @param {Object} res - response object    */   handleRouteNotFound(req, res) {     const errorMessage = 'Route not found'     this.logger.error(errorMessage)     res.status(404).json({ message: errorMessage })   } } // src/app.js /**  * App class for yantraverse framework  */ class App {   /**    * Constructor for App    * @param {Object} logger - logger instance    * @param {Object} errorHandler - error handler instance    */   constructor(logger, errorHandler) {     this.logger = logger     this.errorHandler = errorHandler   }   /**    * Start the app    */   start() {     try {       // start the app     } catch (error) {       this.errorHandler.handleError(error)     }   } } // src/server.js /**  * Server class for yantraverse framework  */ class Server {   /**    * Constructor for Server    * @param {Object} app - app instance    */   constructor(app) {     this.app = app   }   /**    * Handle server error    * @param {Error} error - error to handle    * @param {Object} req - request object    * @param {Object} res - response object    */   handleServerError(error, req, res) {     this.app.errorHandler.handleError(error, req, res)   } } 