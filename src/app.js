   // Import required modules   const express = require('express')   const bodyParser = require('body-parser')   /**    * Create an express application    * @type {Express}    */   const app = express()   /**    * Configure the application to parse JSON and URL-encoded request bodies    */   app.use(bodyParser.json())   app.use(bodyParser.urlencoded({ extended: true }))   /**    * Define a middleware function to parse request bodies    * @param {Request} req - The incoming request object    * @param {Response} res - The outgoing response object    * @param {Function} next - The next middleware function in the stack    */   const parseRequestBody = (req, res, next) => {     try {       // Check if the request body is empty       if (!req.body) {         throw new Error('Request body is empty')       }       // Make the parsed request body available to route handlers       req.parsedBody = req.body       next()     } catch (error) {       // Handle errors during request body parsing       res.status(400).json({ error: 'Failed to parse request body' })     }   }   // Use the middleware function to parse request bodies   app.use(parseRequestBody)   // Define a route handler to test request body parsing   app.post('/test', (req, res) => {     res.json(req.parsedBody)   }) 