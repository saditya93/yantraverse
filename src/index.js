 // src/requestLogger.js /**  * Request Logger class to handle request logging with error handling  */ class RequestLogger {   /**    * Initialize the request logger with a logger function    * @param {function} loggerFunc - function to log requests    */   constructor(loggerFunc) {     this.loggerFunc = loggerFunc   }   /**    * Log a request with relevant information    * @param {object} request - request object    * @param {object} response - response object    * @param {number} responseStatusCode - response status code    */   logRequest(request, response, responseStatusCode) {     try {       const logMessage = {         method: request.method,         path: request.path,         responseStatusCode: responseStatusCode       }       this.loggerFunc(logMessage)     } catch (error) {       // Handle error logging       console.error('Error logging request:', error)     }   }   /**    * Handle failed requests with error handling    * @param {object} request - request object    * @param {object} error - error object    */   handleFailedRequest(request, error) {     try {       const logMessage = {         method: request.method,         path: request.path,         error: error.message       }       this.loggerFunc(logMessage)     } catch (error) {       // Handle error logging       console.error('Error handling failed request:', error)     }   } } // src/requestHandler.js /**  * Request Handler class to handle requests with logging and error handling  */ class RequestHandler {   /**    * Initialize the request handler with a request logger    * @param {RequestLogger} requestLogger - request logger instance    */   constructor(requestLogger) {     this.requestLogger = requestLogger   }   /**    * Handle a request with logging and error handling    * @param {object} request - request object    * @param {object} response - response object    */   handleRequest(request, response) {     try {       // Simulate a successful request       const responseStatusCode = 200       this.requestLogger.logRequest(request, response, responseStatusCode)       response.send('Request successful')     } catch (error) {       this.requestLogger.handleFailedRequest(request, error)       response.status(500).send('Request failed')     }   } } // src/index.js /**  * Main entry point of the application  */ const requestLogger = new RequestLogger((logMessage) => console.log(logMessage)) const requestHandler = new RequestHandler(requestLogger) // Example usage const request = {   method: 'GET',   path: '/example' } const response = {   send: (message) => console.log(message),   status: (statusCode) => ({ send: (message) => console.log(`Status ${statusCode}: ${message}`) }) } requestHandler.handleRequest(request, response)   