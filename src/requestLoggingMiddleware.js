 // Implementation with JSDoc, error handling, all features /**  * Request Logging Middleware  *   * Logs all incoming requests with correct metadata and handles edge cases such as malformed requests.  *   * @param {Object} req - The incoming request object.  * @param {Object} res - The outgoing response object.  * @param {Function} next - The next middleware function in the stack.  */ function requestLoggingMiddleware(req, res, next) {   try {     // Log request metadata     const metadata = {       method: req.method,       path: req.path,       ip: req.ip,       userAgent: req.get('User-Agent')     }     console.log('Request received:', metadata)     // Set response event listener to log response status     res.on('finish', () => {       const responseMetadata = {         status: res.statusCode,         responseTime: new Date().getTime() - req.startTime       }       console.log('Response sent:', responseMetadata)     })     // Call next middleware function in the stack     next()   } catch (error) {     // Handle edge cases such as malformed requests     console.error('Error in request logging middleware:', error)     next(error)   } } // Export the middleware function export default requestLoggingMiddleware 