 // Implementation with JSDoc, error handling, all features class RequestLoggingMiddleware {   /**    * @param {Object} options - Configuration options    * @param {Function} next - Next middleware function    */   constructor(options) {     this.options = options   }   /**    * @param {Object} req - Incoming request object    * @param {Object} res - Outgoing response object    * @param {Function} next - Next middleware function    */   handleRequest(req, res, next) {     try {       const { method, url, headers } = req       const startTime = Date.now()       res.on('finish', () => {         const { statusCode } = res         const endTime = Date.now()         const responseTime = endTime - startTime         // Log request and response details         global.console.log({           method,           url,           headers,           statusCode,           responseTime         })       })       next()     } catch (error) {       global.console.error(error)       next(error)     }   } } 