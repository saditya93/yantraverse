     // src/utils/httpsRedirect.js     /**      * Automatically redirects HTTP requests to HTTPS      * @param {Object} req - The incoming request object      * @param {Object} res - The outgoing response object      * @param {Function} next - The next middleware function in the stack      */     function httpsRedirect(req, res, next) {       if (req.headers['x-forwarded-proto'] === 'http') {         const httpsUrl = `https://${req.get('host')}${req.url}`         return res.redirect(301, httpsUrl)       }       next()     }     // src/app.js     /**      * Main application entry point      */     function app(req, res, next) {       try {         // existing application logic         httpsRedirect(req, res, next)       } catch (error) {         next(error)       }     }     // src/errors.js     /**      * Custom error class for HTTPS redirect errors      * @extends Error      */     class HttpsRedirectError extends Error {       constructor(message) {         super(message)         this.name = 'HttpsRedirectError'       }     }   