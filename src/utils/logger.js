 // src/middleware/errorHandler.js /**  * Error handling middleware to catch and log errors, returning a user-friendly error message  * @param {Error} error - The error to handle  * @param {Object} req - The request object  * @param {Object} res - The response object  * @param {Function} next - The next middleware function  */ export function errorHandler(error, req, res, next) {   const statusCode = error.statusCode || 500   const errorMessage = error.message || 'Internal Server Error'   const errorLog = {     message: errorMessage,     statusCode,     request: {       method: req.method,       url: req.url,       headers: req.headers,       body: req.body     }   }   // Log the error   global.logger.error(errorLog)   // Return a user-friendly error message   res.status(statusCode).json({ message: errorMessage }) } // src/utils/logger.js /**  * Logger utility to log errors  */ export function logger() {   // Implement logging logic here   // For example, you can use a logging library like winston or log4js } 