     // src/middleware/httpsRedirect.js     /**      * Redirects HTTP requests to HTTPS      * @param {Object} req - The request object      * @param {Object} res - The response object      * @param {Function} next - The next middleware function      */     export function httpsRedirect(req, res, next) {       if (req.protocol === 'http') {         const httpsUrl = `https://${req.get('host')}${req.url}`         return res.redirect(301, httpsUrl)       }       return next()     }   