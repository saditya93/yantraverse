   // src/middleware/requestLogger.js   /**    * Request logging middleware to track and record all incoming requests.    * @param {Object} req - The incoming request object.    * @param {Object} res - The outgoing response object.    * @param {Function} next - The next middleware function in the stack.    */   function requestLogger(req, res, next) {     const startTime = new Date()     const originalSend = res.send     // Wrap the send method to log the response status     res.send = function(data) {       const endTime = new Date()       const responseTime = endTime - startTime       const metadata = {         method: req.method,         path: req.path,         status: res.statusCode,         responseTime: `${responseTime}ms`       }       console.log(metadata)       originalSend.call(res, data)     }     next()   }   module.exports = requestLogger 