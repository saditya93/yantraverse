 // src/middleware/requestLogger.js /**  * Request logging middleware  * @param {Object} req - Express request object  * @param {Object} res - Express response object  * @param {Function} next - Express next function  */ const requestLogger = (req, res, next) => {   const start = Date.now()   const originalSend = res.send.bind(res)   // Wrap res.send to log response status code   res.send = (data) => {     const duration = Date.now() - start     const statusCode = res.statusCode     const method = req.method     const url = req.url     const headers = req.headers     // Log request details     console.log({       method,       url,       headers,       statusCode,       duration,       data,     })     // Call original res.send     originalSend(data)   }   // Call next to continue middleware chain   next() } module.exports = requestLogger 