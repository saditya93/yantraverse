   // src/middleware/requestLogger.js   /**    * Request Logger Middleware    * @param {Object} req - Express request object    * @param {Object} res - Express response object    * @param {Function} next - Express next function    */   function requestLogger(req, res, next) {     const startTime = new Date()     const originalSend = res.send     // Override res.send to log response status     res.send = function(data) {       const endTime = new Date()       const responseTime = endTime - startTime       const statusCode = res.statusCode       const method = req.method       const path = req.path       const metadata = {         method,         path,         statusCode,         responseTime,       }       console.log('Request metadata:', metadata)       originalSend.call(res, data)     }     next()   }   module.exports = requestLogger   