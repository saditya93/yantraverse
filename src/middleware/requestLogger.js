   // src/middleware/requestLogger.js   /**    * Request logging middleware    * @param {Object} req - Express request object    * @param {Object} res - Express response object    * @param {Function} next - Express next function    */   export function requestLogger(req, res, next) {     const start = new Date()     const originalSend = res.send     const originalEnd = res.end     // Wrap send and end methods to log response     res.send = (data) => {       logRequest(req, res, start)       originalSend.call(res, data)     }     res.end = (data) => {       logRequest(req, res, start)       originalEnd.call(res, data)     }     next()   }   /**    * Log request information    * @param {Object} req - Express request object    * @param {Object} res - Express response object    * @param {Date} start - Request start time    */   function logRequest(req, res, start) {     const duration = new Date() - start     const statusCode = res.statusCode     const method = req.method     const path = req.path     // Log key request information     console.log(`Method: ${method}, Path: ${path}, Status: ${statusCode}, Duration: ${duration}ms`)   } 