     // src/middleware/requestLogger.js     /**      * Request logging middleware      * @param {Object} req - Incoming request object      * @param {Object} res - Outgoing response object      * @param {Function} next - Next middleware function in the stack      */     export function requestLogger(req, res, next) {       const start_time = new Date()       const method = req.method       const path = req.path       // Create a response event listener to log response metadata       const original_end = res.end       res.end = function (chunk, encoding, callback) {         res.end = original_end         const response_status = res.statusCode         const duration = new Date() - start_time         // Log request metadata         console.log({           method,           path,           response_status,           duration         })         // Call the original response end function         original_end.call(res, chunk, encoding, callback)       }       // Call the next middleware function in the stack       next()     }   