   // Implementation of error handling middleware   /**    * Error handling middleware function    * @param {Object} err - error object    * @param {Object} req - request object    * @param {Object} res - response object    * @param {Function} next - next middleware function    */   const errorHandler = (err, req, res, next) => {     // Log error message and stack trace     console.error(`${err.message}\n${err.stack}`)     // Send user-friendly error message to client     res.status(err.status || 500).json({       message: err.message,       error: 'Internal Server Error'     })   }   // Export error handling middleware   module.exports = errorHandler 