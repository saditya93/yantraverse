   // Import required modules   const logger = require('./logger')   const config = require('./config')   /**    * Error handling middleware    * @param {Object} err - Error object    * @param {Object} req - Request object    * @param {Object} res - Response object    * @param {Function} next - Next middleware function    */   const errorHandler = (err, req, res, next) => {     // Log error with relevant details     logger.error(err.message, {       method: req.method,       url: req.url,       statusCode: err.statusCode || 500,       error: err     })     // Provide a fallback response to the user     res.status(err.statusCode || 500)     res.json({       error: 'Internal Server Error',       message: err.message     })   }   /**    * Route not found handler    * @param {Object} req - Request object    * @param {Object} res - Response object    */   const notFoundHandler = (req, res) => {     // Log route not found error     logger.error('Route not found', {       method: req.method,       url: req.url,       statusCode: 404     })     // Provide a fallback response to the user     res.status(404)     res.json({       error: 'Not Found',       message: 'Route not found'     })   }   // Export error handling middleware   module.exports = {     errorHandler,     notFoundHandler   } 