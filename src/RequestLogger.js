     // Implementation with JSDoc, error handling, all features     /**      * RequestLogger class responsible for logging incoming requests and errors.      */     class RequestLogger {       /**        * Initializes the RequestLogger with a log file path.        * @param {string} logFilePath - The path to the log file.        */       constructor(logFilePath) {         this.logFilePath = logFilePath         this.logFile = null       }       /**        * Enables logging by default and writes logs to a file.        */       enableLogging() {         try {           const fs = require('fs')           this.logFile = fs.createWriteStream(this.logFilePath, { flags: 'a' })         } catch (error) {           console.error('Error initializing log file:', error)         }       }       /**        * Logs a successful request with metadata and response code.        * @param {object} request - The incoming request object.        * @param {number} responseCode - The HTTP response code.        */       logRequest(request, responseCode) {         try {           const logMessage = JSON.stringify({             timestamp: new Date(),             request: {               method: request.method,               url: request.url,               headers: request.headers             },             responseCode           })           this.logFile.write(logMessage + '\n')         } catch (error) {           console.error('Error logging request:', error)         }       }       /**        * Logs an error with the error message and request metadata.        * @param {object} request - The incoming request object.        * @param {string} errorMessage - The error message.        */       logError(request, errorMessage) {         try {           const logMessage = JSON.stringify({             timestamp: new Date(),             request: {               method: request.method,               url: request.url,               headers: request.headers             },             error: errorMessage           })           this.logFile.write(logMessage + '\n')         } catch (error) {           console.error('Error logging error:', error)         }       }     }     module.exports = RequestLogger   