     // Import required modules     const winston = require('winston')     /**      * Logger class for handling critical events      */     class Logger {       /**        * Initialize the logger        */       constructor() {         this.logger = winston.createLogger({           level: 'info',           format: winston.format.json(),           transports: [             new winston.transports.File({ filename: 'error.log', level: 'error' }),             new winston.transports.File({ filename: 'combined.log' })           ]         })       }       /**        * Log server errors        * @param {Error} error - The error to log        */       logServerError(error) {         try {           this.logger.error('Server error: ' + error.message)         } catch (loggingError) {           console.error('Error logging server error: ' + loggingError.message)         }       }       /**        * Log rate limiting incidents        * @param {string} message - The message to log        */       logRateLimitingIncident(message) {         try {           this.logger.info('Rate limiting incident: ' + message)         } catch (loggingError) {           console.error('Error logging rate limiting incident: ' + loggingError.message)         }       }     }     module.exports = Logger   