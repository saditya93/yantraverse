   // Import required modules   const logger = require('./logger')   const logRequest = (req, res, next) => {     // Log request metadata     logger.info({       method: req.method,       url: req.url,       headers: req.headers,       query: req.query,       body: req.body     })     // Handle request     next()     // Log response metadata     logger.info({       method: req.method,       url: req.url,       statusCode: res.statusCode,       responseTime: Date.now() - req.startTime     })   }   // Error handling middleware   const errorHandler = (err, req, res, next) => {     // Log error message     logger.error({       method: req.method,       url: req.url,       error: err.message,       stack: err.stack     })     // Send error response     res.status(500).send({ message: 'Internal Server Error' })   }   // Export middleware functions   module.exports = { logRequest, errorHandler }   