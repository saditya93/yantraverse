 // Import required modules const log4js = require('log4js') const config = require('./config') /**  * Initialize logger with configuration  * @param {Object} config - Logging configuration  */ function initLogger(config) {   log4js.configure(config)   return log4js.getLogger() } /**  * Log incoming request metadata  * @param {Object} req - Request object  * @param {Object} res - Response object  * @param {Function} next - Next middleware function  */ function logRequestMetadata(req, res, next) {   const logger = initLogger(config.logging)   const metadata = {     method: req.method,     url: req.url,     headers: req.headers,     query: req.query,     body: req.body   }   logger.info('Request metadata:', metadata)   next() } /**  * Log errors that occur during request processing  * @param {Error} err - Error object  * @param {Object} req - Request object  * @param {Object} res - Response object  * @param {Function} next - Next middleware function  */ function logError(err, req, res, next) {   const logger = initLogger(config.logging)   logger.error('Request error:', err)   next(err) } module.exports = {   logRequestMetadata,   logError } 