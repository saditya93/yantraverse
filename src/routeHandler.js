   // src/errorHandler.js   /**    * Centralized error handling mechanism for route handlers    * @param {Object} options - Configuration options    * @param {Function} options.customErrorHandler - Custom error handler function    */   export function errorHandler(options) {     const customErrorHandler = options.customErrorHandler     return async (error, req, res, next) => {       try {         // Log the error         console.error(error)         // Check if custom error handler is provided         if (customErrorHandler) {           // Call custom error handler           const errorMessage = await customErrorHandler(error, req, res, next)           res.status(500).json({ message: errorMessage })         } else {           // Default error handling behavior           res.status(500).json({ message: 'Internal Server Error' })         }       } catch (error) {         // Catch and log any errors that occur during error handling         console.error(error)         res.status(500).json({ message: 'Internal Server Error' })       }     }   }   // src/routeHandler.js   /**    * Route handler with error handling    * @param {Object} req - Request object    * @param {Object} res - Response object    * @param {Function} next - Next middleware function    */   export function routeHandler(req, res, next) {     try {       // Route handler logic       const data = { message: 'Hello World' }       res.json(data)     } catch (error) {       // Call error handler       next(error)     }   }   // src/middleware.js   /**    * Middleware function with error handling    * @param {Object} req - Request object    * @param {Object} res - Response object    * @param {Function} next - Next middleware function    */   export function middleware(req, res, next) {     try {       // Middleware logic       req.middlewareData = { message: 'Middleware Data' }       next()     } catch (error) {       // Call error handler       next(error)     }   }   