 // src/retryMechanism.js /**  * Retry mechanism for failed requests  * @param {Object} options - Configuration options  * @param {number} options.maxRetries - Maximum number of retries  * @param {number} options.backoffDelay - Backoff delay in milliseconds  * @param {Function} options.request - Request function to retry  * @returns {Promise} Resolved when the request is successful or fails after max retries  */ async function retryRequest(options) {   const { maxRetries, backoffDelay, request } = options   let retries = 0   while (retries <= maxRetries) {     try {       return await request()     } catch (error) {       if (retries === maxRetries) {         throw error       }       retries++       await new Promise(resolve => globalThis.setTimeout(resolve, backoffDelay))     }   } } export { retryRequest } 