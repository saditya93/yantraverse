 // Implementation with JSDoc, error handling, all features class RequestLogger {   /**    * Logs each incoming request and any errors that occur during processing    * @param {Object} req - The incoming request object    * @param {Object} res - The response object    * @param {Function} next - The next middleware function in the stack    */   logRequest(req, res, next) {     try {       // Log the incoming request       console.log(`Request: ${req.method} ${req.url}`)              // Set a response event listener to log the response       res.on('finish', () => {         console.log(`Response: ${res.statusCode} ${res.statusMessage}`)       })              // Call the next middleware function in the stack       next()     } catch (error) {       // Log any errors that occur during processing       console.error(`Error: ${error.message}`)              // Return a server error response       res.status(500).send('Internal Server Error')     }   } } // Create a new instance of the RequestLogger class const requestLogger = new RequestLogger() // Export the logRequest function export default requestLogger.logRequest 