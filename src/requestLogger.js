   // src/requestLogger.js   /**    * Logs request metadata, response codes, and error messages.    * @param {Object} request - Request object    * @param {Object} response - Response object    * @param {Error} [error] - Error object (optional)    */   export function logRequest(request, response, error = null) {     const metadata = {       method: request.method,       url: request.url,       headers: request.headers,     }     const logData = {       metadata,       responseCode: response.statusCode,       errorMessage: error ? error.message : null,     }     // Log the data     console.log(logData)   }   /**    * Handles request logging with error handling.    * @param {Function} next - Next middleware function    * @param {Object} request - Request object    * @param {Object} response - Response object    */   export function requestLogger(next, request, response) {     try {       // Call the next middleware function       next()     } catch (error) {       // Log the request with error       logRequest(request, response, error)       // Return a server error response       response.statusCode = 500       response.end('Internal Server Error')     }   }   