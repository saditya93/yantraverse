 // src/requestLogger.js /**  * RequestLogger class responsible for logging incoming requests  * @class  */ class RequestLogger {   /**    * Constructor for RequestLogger    * @param {object} options - Configuration options    * @param {string} options.logLevel - Log level (info, debug, error)    * @param {string} options.logOutput - Log output (console, file)    */   constructor({ logLevel = 'info', logOutput = 'console' } = {}) {     this.logLevel = logLevel     this.logOutput = logOutput   }   /**    * Log a request    * @param {object} request - Incoming request object    * @param {number} responseCode - Response code    * @param {string} errorMessage - Error message (if any)    */   logRequest(request, responseCode, errorMessage) {     const logMessage = {       requestMethod: request.method,       requestUrl: request.url,       responseCode,       errorMessage,     }     if (this.logOutput === 'console') {       global.console.log(logMessage)     } else if (this.logOutput === 'file') {       // Implement file logging mechanism       const fs = require('fs')       fs.appendFile('request.log', JSON.stringify(logMessage) + '\n', (err) => {         if (err) {           global.console.error('Error writing to log file:', err)         }       })     }   }   /**    * Handle errors for failed requests    * @param {object} request - Incoming request object    * @param {number} responseCode - Response code    * @param {string} errorMessage - Error message    */   handleError(request, responseCode, errorMessage) {     if (this.logLevel === 'error' || this.logLevel === 'debug') {       this.logRequest(request, responseCode, errorMessage)     }   } } module.exports = RequestLogger 