     // src/requestLogger.js     /**      * RequestLogger class for logging incoming requests      * @class RequestLogger      */     class RequestLogger {       /**        * Constructor for RequestLogger        * @param {Object} options - Configuration options        * @param {String} options.logLevel - Log level (debug, info, warn, error)        * @param {String} options.logOutput - Log output (console, file)        */       constructor(options) {         this.logLevel = options.logLevel || 'info'         this.logOutput = options.logOutput || 'console'       }       /**        * Log a request        * @param {Object} request - Incoming request object        * @param {Object} response - Outgoing response object        * @param {Error} error - Error object (if any)        */       logRequest(request, response, error) {         const logMessage = {           timestamp: new Date(),           method: request.method,           url: request.url,           status: response.statusCode,           error: error ? error.message : null         }         if (this.logLevel === 'debug') {           logMessage.headers = request.headers           logMessage.body = request.body         }         this.log(logMessage)       }       /**        * Log a message        * @param {Object} logMessage - Log message object        */       log(logMessage) {         if (this.logOutput === 'console') {           global.console.log(logMessage)         } else if (this.logOutput === 'file') {           // Implement file logging         }       }     }     module.exports = RequestLogger   