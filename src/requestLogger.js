   // RequestLogger class   class RequestLogger {     /**      * Logs an incoming request      * @param {object} req - The incoming request object      * @param {object} res - The outgoing response object      * @param {function} next - The next middleware function in the stack      */     logRequest(req, res, next) {       try {         // Log the request         console.log(`Request received: ${req.method} ${req.url}`)         // Call the next middleware function         next()       } catch (error) {         // Handle any errors that occur during logging         console.error(`Error logging request: ${error.message}`)         // Call the next middleware function with the error         next(error)       }     }     /**      * Handles errors that occur during request processing      * @param {object} err - The error object      * @param {object} req - The incoming request object      * @param {object} res - The outgoing response object      * @param {function} next - The next middleware function in the stack      */     handleError(err, req, res, next) {       try {         // Log the error         console.error(`Error occurred: ${err.message}`)         // Set the response status code to 500 (Internal Server Error)         res.statusCode = 500         // Send a JSON response with the error message         res.json({ message: 'Internal Server Error' })       } catch (error) {         // Handle any errors that occur during error handling         console.error(`Error handling error: ${error.message}`)         // Call the next middleware function with the error         next(error)       }     }   }   // Export the RequestLogger class   module.exports = RequestLogger 