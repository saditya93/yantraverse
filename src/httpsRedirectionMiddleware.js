     // Import required modules     const http = require('http')     const https = require('https')     /**      * Automatically redirects HTTP requests to HTTPS      * @param {http.IncomingMessage} req - The incoming HTTP request      * @param {http.ServerResponse} res - The outgoing HTTP response      * @param {function} next - The next middleware function in the stack      */     function httpsRedirectionMiddleware(req, res, next) {       // Check if the request protocol is HTTP       if (req.protocol === 'http') {         // Redirect to HTTPS         const httpsUrl = `https://${req.get('host')}${req.url}`         res.redirect(301, httpsUrl)       } else {         // If the request is already HTTPS, continue to the next middleware         next()       }     }     // Export the middleware function     module.exports = httpsRedirectionMiddleware   