 // test/middleware/errorHandler.test.js import { errorHandler } from '../src/middleware/errorHandler' import { logger } from '../src/utils/logger' describe('Error Handler Middleware', () => {   it('should catch and log synchronous errors', () => {     const error = new Error('Test Error')     const req = { method: 'GET', url: '/test', headers: {}, body: {} }     const res = { status: jest.fn().mockReturnThis(), json: jest.fn() }     const next = jest.fn()     errorHandler(error, req, res, next)     expect(res.status).toHaveBeenCalledTimes(1)     expect(res.status).toHaveBeenCalledWith(500)     expect(res.json).toHaveBeenCalledTimes(1)     expect(res.json).toHaveBeenCalledWith({ message: 'Test Error' })     expect(logger.error).toHaveBeenCalledTimes(1)     expect(logger.error).toHaveBeenCalledWith({       message: 'Test Error',       statusCode: 500,       request: {         method: 'GET',         url: '/test',         headers: {},         body: {}       }     })   })   it('should catch and log asynchronous errors', async () => {     const error = new Error('Test Error')     const req = { method: 'GET', url: '/test', headers: {}, body: {} }     const res = { status: jest.fn().mockReturnThis(), json: jest.fn() }     const next = jest.fn()     await errorHandler(error, req, res, next)     expect(res.status).toHaveBeenCalledTimes(1)     expect(res.status).toHaveBeenCalledWith(500)     expect(res.json).toHaveBeenCalledTimes(1)     expect(res.json).toHaveBeenCalledWith({ message: 'Test Error' })     expect(logger.error).toHaveBeenCalledTimes(1)     expect(logger.error).toHaveBeenCalledWith({       message: 'Test Error',       statusCode: 500,       request: {         method: 'GET',         url: '/test',         headers: {},         body: {}       }     })   })   it('should log error with default status code and message', () => {     const error = new Error()     const req = { method: 'GET', url: '/test', headers: {}, body: {} }     const res = { status: jest.fn().mockReturnThis(), json: jest.fn() }     const next = jest.fn()     errorHandler(error, req, res, next)     expect(res.status).toHaveBeenCalledTimes(1)     expect(res.status).toHaveBeenCalledWith(500)     expect(res.json).toHaveBeenCalledTimes(1)     expect(res.json).toHaveBeenCalledWith({ message: 'Internal Server Error' })     expect(logger.error).toHaveBeenCalledTimes(1)     expect(logger.error).toHaveBeenCalledWith({       message: 'Internal Server Error',       statusCode: 500,       request: {         method: 'GET',         url: '/test',         headers: {},         body: {}       }     })   }) } 