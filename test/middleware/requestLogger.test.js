   // test/middleware/requestLogger.test.js   const requestLogger = require('../../src/middleware/requestLogger')   const express = require('express')   const sinon = require('sinon')   const assert = require('assert')   describe('Request Logger Middleware', () => {     it('logs requests correctly', (done) => {       const app = express()       const req = {         method: 'GET',         path: '/test',       }       const res = {         send: sinon.stub(),         statusCode: 200,       }       const next = sinon.stub()       requestLogger(req, res, next)       res.send('Test response')       assert(next.calledOnce)       assert(res.send.calledOnce)       done()     })     it('handles edge cases without errors', (done) => {       const app = express()       const req = {         method: 'GET',         path: '/test',       }       const res = {         send: sinon.stub(),         statusCode: 500,       }       const next = sinon.stub()       requestLogger(req, res, next)       res.send('Test response')       assert(next.calledOnce)       assert(res.send.calledOnce)       done()     })   })   