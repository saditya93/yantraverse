     // test/route-validator.test.js     import { validateAndHandleErrors } from '../src/route-validator'     describe('validateAndHandleErrors', () => {       it('should validate valid route parameters', () => {         const params = { id: '123', name: 'John Doe' }         const rules = { id: { type: 'string' }, name: { type: 'string' } }         const result = validateAndHandleErrors(params, rules)         expect(result).toEqual(params)       })       it('should handle missing required parameter', () => {         const params = { name: 'John Doe' }         const rules = { id: { type: 'string', required: true }, name: { type: 'string' } }         const result = validateAndHandleErrors(params, rules)         expect(result).toEqual({ error: 'Missing required parameter: id' })       })       it('should handle invalid type for parameter', () => {         const params = { id: '123', name: 123 }         const rules = { id: { type: 'string' }, name: { type: 'string' } }         const result = validateAndHandleErrors(params, rules)         expect(result).toEqual({ error: 'Invalid type for parameter name: expected string, got number' })       })       it('should handle parameter length validation', () => {         const params = { id: '123', name: 'a' }         const rules = { id: { type: 'string' }, name: { type: 'string', minLength: 2 } }         const result = validateAndHandleErrors(params, rules)         expect(result).toEqual({ error: 'Parameter name must be at least 2 characters long' })       })     })   