 // Import required modules const express = require('express') const request = require('supertest') const app = require('../src/file') // Test JSON body parsing describe('JSON body parsing', () => {   it('should parse JSON request bodies correctly', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/json')       .send({ foo: 'bar' })     expect(res.status).toBe(200)     expect(res.body).toEqual({ foo: 'bar' })   })   it('should return a 400 error for invalid JSON request bodies', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/json')       .send('Invalid JSON')     expect(res.status).toBe(400)     expect(res.body).toEqual({ error: 'Invalid JSON request body' })   }) }) // Test URL-encoded body parsing describe('URL-encoded body parsing', () => {   it('should parse URL-encoded request bodies correctly', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/x-www-form-urlencoded')       .send('foo=bar')     expect(res.status).toBe(200)     expect(res.body).toEqual({ foo: 'bar' })   })   it('should return a 400 error for invalid URL-encoded request bodies', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/x-www-form-urlencoded')       .send('Invalid URL-encoded data')     expect(res.status).toBe(400)     expect(res.body).toEqual({ error: 'Invalid URL-encoded request body' })   }) }) // Test unsupported media types describe('Unsupported media types', () => {   it('should return a 415 error for unsupported media types', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'text/plain')       .send('Hello, world!')     expect(res.status).toBe(415)     expect(res.body).toEqual({ error: 'Unsupported media type' })   }) }) 