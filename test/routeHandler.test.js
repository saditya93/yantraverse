   // test/errorHandler.test.js   import { errorHandler } from '../src/errorHandler'   import { routeHandler } from '../src/routeHandler'   import { middleware } from '../src/middleware'   describe('Error Handler', () => {     it('should catch and log errors', async () => {       const error = new Error('Test Error')       const req = {}       const res = { status: jest.fn().mockReturnThis(), json: jest.fn() }       const next = jest.fn()       await errorHandler({})(error, req, res, next)       expect(next).not.toHaveBeenCalled()       expect(res.status).toHaveBeenCalledTimes(1)       expect(res.status).toHaveBeenCalledWith(500)       expect(res.json).toHaveBeenCalledTimes(1)       expect(res.json).toHaveBeenCalledWith({ message: 'Internal Server Error' })     })     it('should call custom error handler', async () => {       const error = new Error('Test Error')       const req = {}       const res = { status: jest.fn().mockReturnThis(), json: jest.fn() }       const next = jest.fn()       const customErrorHandler = jest.fn().mockReturnValue('Custom Error Message')       await errorHandler({ customErrorHandler })(error, req, res, next)       expect(next).not.toHaveBeenCalled()       expect(res.status).toHaveBeenCalledTimes(1)       expect(res.status).toHaveBeenCalledWith(500)       expect(res.json).toHaveBeenCalledTimes(1)       expect(res.json).toHaveBeenCalledWith({ message: 'Custom Error Message' })     })   })   describe('Route Handler', () => {     it('should catch and log errors', async () => {       const error = new Error('Test Error')       const req = {}       const res = { json: jest.fn() }       const next = jest.fn()       routeHandler(req, res, next)       expect(next).toHaveBeenCalledTimes(1)       expect(next).toHaveBeenCalledWith(error)     })   })   describe('Middleware', () => {     it('should catch and log errors', async () => {       const error = new Error('Test Error')       const req = {}       const res = {}       const next = jest.fn()       middleware(req, res, next)       expect(next).toHaveBeenCalledTimes(1)       expect(next).toHaveBeenCalledWith(error)     })   })   