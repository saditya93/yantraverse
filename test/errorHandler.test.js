 // test/requestLogger.test.js const request = require('supertest') const app = require('../src/app') describe('Request logging middleware', () => {   it('logs successful requests', async () => {     const res = await request(app).get('/')     expect(res.statusCode).toBe(200)   })   it('logs failed requests with error handling', async () => {     const res = await request(app).get('/non-existent-route')     expect(res.statusCode).toBe(500)   }) }) // test/errorHandler.test.js const request = require('supertest') const app = require('../src/app') describe('Error handler', () => {   it('handles errors without crashing the application', async () => {     const res = await request(app).get('/error')     expect(res.statusCode).toBe(500)   }) }) 