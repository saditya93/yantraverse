     // test/requestLogger.test.js     const RequestLogger = require('../src/requestLogger')     const assert = require('assert')     describe('RequestLogger', () => {       it('should log a successful request', () => {         const requestLogger = new RequestLogger({ logLevel: 'info' })         const request = { method: 'GET', url: '/test', headers: {}, body: {} }         const response = { statusCode: 200 }         requestLogger.logRequest(request, response, null)         // Assert log message       })       it('should log a failed request', () => {         const requestLogger = new RequestLogger({ logLevel: 'info' })         const request = { method: 'GET', url: '/test', headers: {}, body: {} }         const response = { statusCode: 500 }         const error = new Error('Test error')         requestLogger.logRequest(request, response, error)         // Assert log message       })       it('should configure log level', () => {         const requestLogger = new RequestLogger({ logLevel: 'debug' })         assert.strictEqual(requestLogger.logLevel, 'debug')       })       it('should configure log output', () => {         const requestLogger = new RequestLogger({ logOutput: 'file' })         assert.strictEqual(requestLogger.logOutput, 'file')       })     })   