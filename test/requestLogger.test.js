   // Import the RequestLogger class   const RequestLogger = require('./file')   const requestLogger = new RequestLogger()   // Test logging with a valid request   it('logs a valid request', () => {     const req = { method: 'GET', url: '/test' }     const res = {}     const next = jest.fn()     requestLogger.logRequest(req, res, next)     expect(console.log).toHaveBeenCalledTimes(1)     expect(next).toHaveBeenCalledTimes(1)   })   // Test logging with an invalid request   it('logs an invalid request', () => {     const req = { method: 'INVALID', url: '/test' }     const res = {}     const next = jest.fn()     requestLogger.logRequest(req, res, next)     expect(console.log).toHaveBeenCalledTimes(1)     expect(next).toHaveBeenCalledTimes(1)   })   // Test error handling with an unexpected error   it('handles an unexpected error', () => {     const err = new Error('Unexpected error')     const req = {}     const res = { statusCode: 0, json: jest.fn() }     const next = jest.fn()     requestLogger.handleError(err, req, res, next)     expect(console.error).toHaveBeenCalledTimes(1)     expect(res.statusCode).toBe(500)     expect(res.json).toHaveBeenCalledTimes(1)   }) 