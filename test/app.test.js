 // Import required modules const request = require('supertest') const app = require('../src/app') // Test that HTTP requests are redirected to HTTPS describe('HTTP request redirect', () => {   it('should redirect to HTTPS', async () => {     const res = await request(app).get('/').set('Host', 'example.com')     expect(res.header['location']).toBe('https://example.com/')   }) }) // Test that HTTPS requests are not redirected describe('HTTPS request', () => {   it('should not redirect', async () => {     const res = await request(app).get('/').set('Host', 'example.com').set('x-forwarded-proto', 'https')     expect(res.header['location']).toBeUndefined()   }) }) 