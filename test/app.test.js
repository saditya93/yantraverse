 // Import required modules const request = require('supertest') const app = require('../src/app') // Test JSON body parsing describe('JSON body parsing', () => {   it('should parse JSON body correctly', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/json')       .send({ foo: 'bar' })     expect(res.body).toEqual({ foo: 'bar' })   })   it('should handle invalid JSON', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/json')       .send('Invalid JSON')     expect(res.status).toBe(400)     expect(res.body).toEqual({ error: 'Invalid request body' })   }) }) // Test URL-encoded body parsing describe('URL-encoded body parsing', () => {   it('should parse URL-encoded body correctly', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/x-www-form-urlencoded')       .send('foo=bar')     expect(res.body).toEqual({ foo: 'bar' })   })   it('should handle invalid URL-encoded data', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'application/x-www-form-urlencoded')       .send('%Invalid%URL-encoded%data')     expect(res.status).toBe(400)     expect(res.body).toEqual({ error: 'Invalid request body' })   }) }) // Test unsupported content type describe('Unsupported content type', () => {   it('should return 400 for unsupported content type', async () => {     const res = await request(app)       .post('/test')       .set('Content-Type', 'text/plain')       .send('Hello World')     expect(res.status).toBe(400)     expect(res.body).toEqual({ error: 'Invalid request body' })   }) }) 