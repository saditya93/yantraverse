   // Import required modules   const request = require('supertest')   const app = require('../src/app')   // Test JSON body parsing   it('should parse JSON request bodies', async () => {     const response = await request(app)       .post('/test')       .set('Content-Type', 'application/json')       .send({ key: 'value' })     expect(response.status).toBe(200)     expect(response.body).toEqual({ key: 'value' })   })   // Test URL-encoded body parsing   it('should parse URL-encoded request bodies', async () => {     const response = await request(app)       .post('/test')       .set('Content-Type', 'application/x-www-form-urlencoded')       .send('key=value')     expect(response.status).toBe(200)     expect(response.body).toEqual({ key: 'value' })   })   // Test body parsing error handling   it('should handle errors during request body parsing', async () => {     const response = await request(app)       .post('/test')       .set('Content-Type', 'text/plain')       .send('Invalid request body')     expect(response.status).toBe(400)     expect(response.body).toEqual({ error: 'Failed to parse request body' })   }) 