     // Import required modules     const request = require('supertest')     const app = require('../src/app')     // Test error handling for route not found     it('should return a 404 status code for a non-existent route', async () => {       const response = await request(app).get('/non-existent-route')       expect(response.status).toBe(404)     })     // Test error handling for middleware errors     it('should return a 500 status code for a middleware error', async () => {       const response = await request(app).get('/middleware-error')       expect(response.status).toBe(500)     })     // Test error handling for server errors     it('should return a 500 status code for a server error', async () => {       const response = await request(app).get('/server-error')       expect(response.status).toBe(500)     })   