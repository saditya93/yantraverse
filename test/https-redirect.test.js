     // Import required modules     const http = require('http')     const assert = require('assert')     const url = require('url')     // Test redirect from HTTP to HTTPS     it('should redirect from HTTP to HTTPS', (done) => {       const options = {         hostname: 'localhost',         port: 80,         path: '/',         method: 'GET'       }       const req = http.request(options, (res) => {         assert.equal(res.statusCode, 301)         assert.equal(res.headers.location, 'https://localhost/')         done()       })       req.end()     })     // Test redirect with query parameters     it('should redirect with query parameters', (done) => {       const options = {         hostname: 'localhost',         port: 80,         path: '/?param1=value1&param2=value2',         method: 'GET'       }       const req = http.request(options, (res) => {         assert.equal(res.statusCode, 301)         assert.equal(res.headers.location, 'https://localhost/?param1=value1&param2=value2')         done()       })       req.end()     })     // Test redirect with HTTP headers     it('should redirect with HTTP headers', (done) => {       const options = {         hostname: 'localhost',         port: 80,         path: '/',         method: 'GET',         headers: {           'Custom-Header': 'custom-value'         }       }       const req = http.request(options, (res) => {         assert.equal(res.statusCode, 301)         assert.equal(res.headers.location, 'https://localhost/')         done()       })       req.end()     })   