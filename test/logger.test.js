 // Import required modules const request = require('supertest') const logger = require('../src/logger') const app = require('../src/app') /**  * Test logging of successful requests  */ describe('Successful requests', () => {   it('should log request metadata', async () => {     const res = await request(app).get('/success')     expect(res.statusCode).toBe(200)     expect(logger.logRequestMetadata).toHaveBeenCalledTimes(1)   }) }) /**  * Test logging of requests with errors  */ describe('Requests with errors', () => {   it('should log error messages', async () => {     const res = await request(app).get('/error')     expect(res.statusCode).toBe(500)     expect(logger.logError).toHaveBeenCalledTimes(1)   }) }) /**  * Test logging configuration options  */ describe('Logging configuration', () => {   it('should use default logging configuration', () => {     const logger = logger.initLogger()     expect(logger.level).toBe('info')   })   it('should use custom logging configuration', () => {     const customConfig = {       level: 'debug'     }     const logger = logger.initLogger(customConfig)     expect(logger.level).toBe('debug')   }) }) 