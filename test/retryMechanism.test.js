 // test/retryMechanism.test.js import { retryRequest } from '../src/retryMechanism' describe('retryRequest', () => {   it('should retry the request after a single failure', async () => {     let attempts = 0     const request = () => {       attempts++       if (attempts === 1) {         throw new Error('Temporary error')       }       return 'Success'     }     const options = { maxRetries: 3, backoffDelay: 100, request }     const result = await retryRequest(options)     expect(result).toBe('Success')     expect(attempts).toBe(2)   })   it('should fail after maximum retries exceeded', async () => {     let attempts = 0     const request = () => {       attempts++       throw new Error('Temporary error')     }     const options = { maxRetries: 3, backoffDelay: 100, request }     await expect(retryRequest(options)).rejects.toThrow('Temporary error')     expect(attempts).toBe(4)   }) }) 